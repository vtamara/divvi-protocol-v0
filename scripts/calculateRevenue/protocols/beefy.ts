import { RevenueResult } from '../../types'

export async function calculateRevenue(_params: {
  address: string
  startTimestamp: Date
  endTimestamp: Date
}): Promise<RevenueResult> {
  return {}
}
